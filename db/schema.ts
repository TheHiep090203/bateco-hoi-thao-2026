import { sqliteTable, text, integer, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const alliances = sqliteTable('alliances', {id:integer('id').primaryKey(),name:text('name').notNull(),members:text('members').notNull(),gold:integer('gold').notNull().default(0),silver:integer('silver').notNull().default(0),bronze:integer('bronze').notNull().default(0)});
export const sports = sqliteTable('sports', {id:integer('id').primaryKey(),name:text('name').notNull(),discipline:text('discipline').notNull()});
export const results = sqliteTable('results', {id:text('id').primaryKey(),sportId:integer('sport_id').notNull().references(()=>sports.id),event:text('event').notNull(),participants:text('participants').notNull(),score:text('score').notNull(),gold:integer('gold').references(()=>alliances.id),silver:integer('silver').references(()=>alliances.id),bronze:integer('bronze').references(()=>alliances.id),revision:integer('revision').notNull().default(1),updatedAt:text('updated_at').notNull(),updatedBy:text('updated_by').notNull()},t=>[uniqueIndex('results_sport_event').on(t.sportId,t.event)]);
export const draws = sqliteTable('draws', {id:text('id').primaryKey(),sportId:integer('sport_id').notNull().references(()=>sports.id),ord:integer('ord').notNull(),c1:text('c1').notNull().default(''),c2:text('c2').notNull().default(''),c3:text('c3').notNull().default(''),c4:text('c4').notNull().default('')},t=>[index('draws_sport_ord').on(t.sportId,t.ord)]);
export const brackets = sqliteTable('brackets', {sportId:integer('sport_id').primaryKey().references(()=>sports.id),data:text('data').notNull(),updatedAt:text('updated_at').notNull()});
export const rules = sqliteTable('rules', {ruleKey:text('rule_key').primaryKey(),data:text('data').notNull(),updatedAt:text('updated_at').notNull()});
